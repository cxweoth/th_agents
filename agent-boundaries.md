# Agent 的權責邊界

這個檔記錄「一個 agent 該不該同時擁有兩層」這類問題。跟 `candidates.md` 分開，
因為那個檔管的是 skill 提案，這個檔管的是**要幾個 agent、各自擁有什麼**。

---

## 2026-08-08 — 同一個 agent 無法對自己分層

**TH 的觀察**：dozo 的 src agent 底下有一個已經是 module 等級的東西：給它一份
observation 與 reward 的 config，它就照著跑 training 與 simulate。現在要跑多個
seed，正確做法只是**把那個 module 指向不同資料夾、換不同 seed**，module 本身一行
都不該動。實際發生的是：src 為了滿足上層的需要去動 module 內部，或把不該放在裡面
的程式塞進去。

**TH 的提議**：另開一個 agent 專門維護那個 module，把權責分開。

**TH 指出的人類對應**：同一個人開發一整套系統時，系統裡的 sub module 一樣會權責
不清。

### 機制

不是懶惰，是**沒有反向利益**。接口的守衛與接口的破壞者是同一方，所以破壞接口沒有
阻力。一個接口能維持成接口，靠的不是紀律，是**有人會因為它被破壞而損失**。

### ★ 這個系統裡已經有一個成功的先例

`northstar-bridge` 做的是同一件事，只是高一層：Planner 不開專案本體，RA 是唯一
大使，跨界的每一次要求都得寫成 directive。**TH 的提議是把已經驗證過的模式再往下
套一層。**

### 代價（先看見再決定）

1. **TH 自己的時間。** 2026-07-27 實測：指揮一個 RA 一輪約 2 小時，三個 RA 約一整天，
   同時最多切換兩個 project。給 dozo 加第三個 agent 等於往那條線推。
2. **module agent 看不到「為什麼」**，只看得到請求，會在「擋掉好的改動」與「橡皮
   圖章」之間擺盪。bridge 是用 `--- NORTHSTAR CONTEXT ---` 區塊解決的，同一個解法
   要往下複製。
3. **每次跨界變成一次協商**，慢。

### 早期警訊（出現就代表分層失敗）

- module agent 什麼都批准：變成橡皮圖章，只多了成本沒有多防護
- 上層開始在 module 外面**複製** module 的邏輯以避開協商：比原本更糟，因為變成兩份
  會漂移的實作

### 比較便宜的替代方案

不加 agent，改成要求 module 有一份寫下來的接口契約，任何要動 module 內部的改動必須
先說明「為什麼上層的需要無法透過現有接口達成」。成本低很多，防護也弱很多，因為說明
的人跟受益的人還是同一個。

### 狀態

TH 說「幫我記住，然後我想改善它」。**尚未決定要不要做**，也還沒問過 dozo RA 那個
module 的接口現在長什麼樣。
